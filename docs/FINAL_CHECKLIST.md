# Final checklist — the only steps left, in order

Everything else is done, deployed, and verified (last checked: all 5 services live,
`git` fully synced with `origin/master`). This is the complete, ordered list of what's
actually left, with no decisions left to make — just execution.

## 1. Bazantic Recipe (~10 minutes)

1. Log into bazantic.com.
2. Open **Recipes → New**.
3. Copy every field from `docs/BAZANTIC_RECIPE_DRAFT.md` verbatim — name, description,
   steps, example. Pick whichever already-listed service (PurpleAir or Api Ninjas) you see
   live in your dashboard for the "fetch" half; the draft explains why either works.
4. Bind the tools: your `deep.wayfare.eth` gateway (slug `tgbve4lyhrcczglh637jp2kvqu`) plus
   the other service's gateway, per the draft's step-by-step.
5. Open the Playground, run it once for real, and screen-record that run — the checklist
   at the bottom of `docs/BAZANTIC_RECIPE_DRAFT.md` says exactly what to capture.
6. Note your Bazantic account username/email — both prize submissions require it.

## 2. Narration (however long your friend needs)

1. Send them `wayfare-final-3min.mp4` and `wayfare-final-script.md` (both already in
   `Downloads/`).
2. They record voice over the existing footage using that script's timestamps as a guide.
3. Export the narrated cut.

## 3. Fill in the four sponsor submissions

`docs/SUBMISSIONS.md` has full draft text for all four, copy-paste ready:

- Hedera — AI & Agentic Payments
- ENS — Best Use of ENSv2
- Bazantic — Agentify a New API
- Bazantic — Best Recipe Using Sponsor APIs

For each: paste the writeup, attach the narrated video from step 2 (Bazantic's two entries
also need the short Playground clip from step 1), and submit.

## Nothing else is outstanding

Every other checkbox in `README.md`'s Status section is real and verified — no other
loose ends are known at this time.
