# deep (not yet built)

Planned for M4 (Choice). Thorough text-summarization result, higher price metered by
input size. Should mirror `apps/providers/swift`'s structure (health / quote / execute /
agent-card, gated the same way via `@x402/express` + Blocky402) but with a real
metered price and a visibly better summary method than swift's lead-sentence extraction
(e.g. sentence-ranking extractive summarization over the whole input).
