# receipts (not yet built)

Planned for M6 (Receipts). One HCS topic. Each settled call writes: provider
ENS name, quote id, amount, Hedera tx id, result hash, timestamp. Mirror Node
confirms. The frontend links each receipt to HashScan.
